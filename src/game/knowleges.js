export const KNOWLEDGE = {
  ASTROLOGY: "astrology",
  ALCHEMY: "alchemy",
  ENGINEERING: "engineering",
};

/**
 * Get knowledge recort from store or null
 *
 * @param {object} kstore - dictionary with knowledges
 * @param {string} name key name for knowlege
 * @returns knowledge object
 */
export function getKnowledge(kstore, name) {
  return null;
}
